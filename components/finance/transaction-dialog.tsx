"use client";

import { useEffect, useMemo, useState } from "react";
import { format } from "date-fns";
import { ArrowLeftRight, ReceiptText } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Field, Input, Modal, Segmented, Select, Textarea } from "@/components/ui/primitives";
import { useFinance } from "@/components/finance/finance-provider";
import { financeStore } from "@/lib/finance/store";
import { transactionSchema, transferSchema } from "@/lib/finance/validation";
import { defaultPaymentMethodForAccount, paymentMethods } from "@/lib/finance/payment-methods";
import type { PaymentMethod, Transaction } from "@/lib/finance/types";

const today = () => format(new Date(), "yyyy-MM-dd");
export function TransactionDialog({ open, onClose, transaction }: { open: boolean; onClose: () => void; transaction?: Transaction }) {
  const { accounts, categories, run } = useFinance();
  const [mode, setMode] = useState<"entry" | "transfer">(transaction?.type === "transfer" ? "transfer" : "entry");
  const [form, setForm] = useState({ account_id: transaction?.account_id ?? "", category_id: transaction?.category_id ?? "", type: transaction?.type === "income" ? "income" : "expense", amount: transaction?.amount?.toString() ?? "", description: transaction?.description ?? "", transaction_date: transaction?.transaction_date ?? today(), payment_method: transaction?.payment_method ?? "", need_want: transaction?.need_want ?? "need", notes: transaction?.notes ?? "", is_recurring: transaction?.is_recurring ?? false, recurrence_interval: transaction?.recurrence_interval ?? "monthly", next_due_date: transaction?.next_due_date ?? "" });
  const [transfer, setTransfer] = useState({ source_account_id: "", destination_account_id: "", amount: "", description: "", transaction_date: today(), notes: "" });
  const activeAccounts = useMemo(() => accounts.filter((account) => !account.is_archived), [accounts]);
  const [formError, setFormError] = useState("");
  const recommendedMethod = (accountId: string) => {
    const account = activeAccounts.find((item) => item.id === accountId);
    const saved = typeof window === "undefined" ? null : window.localStorage.getItem(`finwise:last-payment-method:${accountId}`) as PaymentMethod | null;
    return defaultPaymentMethodForAccount(account?.type, paymentMethods.includes(saved as PaymentMethod) ? saved : null) ?? "";
  };
  useEffect(() => {
    if (open && !transaction) {
      const initialAccount = activeAccounts[0];
      setForm({ account_id: initialAccount?.id ?? "", category_id: "", type: "expense", amount: "", description: "", transaction_date: today(), payment_method: initialAccount ? recommendedMethod(initialAccount.id) : "", need_want: "need", notes: "", is_recurring: false, recurrence_interval: "monthly", next_due_date: "" });
      setFormError("");
    }
  }, [open, transaction, activeAccounts]);
  const selectedAccount = activeAccounts.find((account) => account.id === form.account_id);
  const set = (key: keyof typeof form, value: string | boolean) => { setFormError(""); setForm((current) => ({ ...current, [key]: value })); };
  const selectAccount = (accountId: string) => { setFormError(""); setForm((current) => ({ ...current, account_id: accountId, payment_method: recommendedMethod(accountId) })); };
  const saveEntry = async () => {
    if (!selectedAccount) return setFormError("Choose the account used for this transaction.");
    if (form.type === "expense" && selectedAccount.type !== "cash" && !form.payment_method) return setFormError("Select the payment method used for this non-cash account.");
    const payment_method = form.type === "expense" ? selectedAccount.type === "cash" ? "cash" : form.payment_method || null : null;
    const parsed = transactionSchema.safeParse({ ...form, amount: Number(form.amount), category_id: form.category_id || null, payment_method, need_want: form.need_want || null, notes: form.notes || null, recurrence_interval: form.is_recurring ? form.recurrence_interval : null, next_due_date: form.is_recurring ? form.next_due_date || null : null });
    if (!parsed.success) return setFormError(parsed.error.issues[0]?.message ?? "Check the transaction details.");
    const result = await run(() => financeStore.saveTransaction(parsed.data, transaction?.id), transaction ? "Transaction updated." : "Transaction recorded.");
    if (result) { if (payment_method) window.localStorage.setItem(`finwise:last-payment-method:${selectedAccount.id}`, payment_method); onClose(); }
  };
  const saveTransfer = async () => { const parsed = transferSchema.safeParse({ ...transfer, amount: Number(transfer.amount), notes: transfer.notes || null }); if (!parsed.success) return; const result = await run(() => financeStore.createTransfer(parsed.data), "Transfer recorded. Balances are updated."); if (result) onClose(); };
  return <Modal open={open} onClose={onClose} title={transaction ? "Edit transaction" : mode === "transfer" ? "Move money" : "Add transaction"}>{!transaction && <div className="mb-6"><Segmented value={mode} onChange={(value) => setMode(value as "entry" | "transfer")} options={[{ value: "entry", label: "Income or expense" }, { value: "transfer", label: "Transfer" }]} /></div>}{mode === "entry" ? <div className="grid gap-4"><div className="grid grid-cols-2 gap-3"><Field label="Type"><Select value={form.type} onChange={(event) => set("type", event.target.value)}><option value="expense">Expense</option><option value="income">Income</option></Select></Field><Field label="Amount"><Input autoFocus inputMode="decimal" value={form.amount} onChange={(event) => set("amount", event.target.value)} placeholder="0.00" /></Field></div><Field label="Description"><Input value={form.description} onChange={(event) => set("description", event.target.value)} placeholder="Coffee, salary, electricity…" /></Field><div className="grid grid-cols-2 gap-3"><Field label="Account"><Select value={form.account_id} onChange={(event) => selectAccount(event.target.value)}><option value="">Choose account</option>{activeAccounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</Select></Field><Field label="Date"><Input type="date" value={form.transaction_date} onChange={(event) => set("transaction_date", event.target.value)} /></Field></div>{form.type === "expense" && <><div className="grid grid-cols-2 gap-3"><Field label="Category"><Select value={form.category_id} onChange={(event) => set("category_id", event.target.value)}><option value="">Uncategorised</option>{categories.filter((category) => !category.is_archived).map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</Select></Field><Field label="Payment method" hint={selectedAccount?.type === "cash" ? "Cash Wallet selected — Cash is applied automatically." : "Select the payment channel used."}><Select value={form.payment_method} onChange={(event) => set("payment_method", event.target.value)} disabled={selectedAccount?.type === "cash"}><option value="">Select payment method</option>{paymentMethods.map((method) => <option key={method} value={method}>{method.split("_").map((word) => word[0].toUpperCase() + word.slice(1)).join(" ")}</option>)}</Select></Field><Field label="Intent"><Select value={form.need_want} onChange={(event) => set("need_want", event.target.value)}><option value="need">Need</option><option value="planned_want">Planned want</option><option value="impulse">Impulse</option></Select></Field></div>{selectedAccount?.type !== "cash" && <p className="-mt-1 text-xs leading-5 text-[var(--muted)]">For bank, card, and online accounts, choose UPI, card, bank transfer, or another method so spending analysis stays accurate.</p>}</>}<label className="flex items-center gap-2 rounded-xl bg-[var(--raised)] px-3 py-2.5 text-sm text-[var(--ink)]"><input type="checkbox" checked={form.is_recurring} onChange={(event) => set("is_recurring", event.target.checked)} className="accent-lime-300" />Mark as recurring</label>{form.is_recurring && <div className="grid grid-cols-2 gap-3"><Field label="Repeat"><Select value={form.recurrence_interval} onChange={(event) => set("recurrence_interval", event.target.value)}><option value="weekly">Weekly</option><option value="monthly">Monthly</option><option value="yearly">Yearly</option></Select></Field><Field label="Next due"><Input type="date" value={form.next_due_date} onChange={(event) => set("next_due_date", event.target.value)} /></Field></div>}<Field label="Notes" hint="Optional"><Textarea value={form.notes} onChange={(event) => set("notes", event.target.value)} placeholder="Add a useful detail…" /></Field>{formError && <p role="alert" className="rounded-xl border border-amber-300/20 bg-amber-300/10 px-3 py-2 text-sm text-amber-100">{formError}</p>}<Button size="lg" onClick={saveEntry}><ReceiptText size={17} />{transaction ? "Save changes" : "Save transaction"}</Button></div> : <div className="grid gap-4"><p className="rounded-xl border border-blue-300/15 bg-blue-300/10 px-3 py-2.5 text-xs leading-5 text-blue-100">Transfers change account balances but are never counted as spending.</p><Field label="From"><Select value={transfer.source_account_id} onChange={(event) => setTransfer((current) => ({ ...current, source_account_id: event.target.value }))}><option value="">Choose source account</option>{activeAccounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</Select></Field><Field label="To"><Select value={transfer.destination_account_id} onChange={(event) => setTransfer((current) => ({ ...current, destination_account_id: event.target.value }))}><option value="">Choose destination account</option>{activeAccounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</Select></Field><div className="grid grid-cols-2 gap-3"><Field label="Amount"><Input autoFocus inputMode="decimal" value={transfer.amount} onChange={(event) => setTransfer((current) => ({ ...current, amount: event.target.value }))} placeholder="0.00" /></Field><Field label="Date"><Input type="date" value={transfer.transaction_date} onChange={(event) => setTransfer((current) => ({ ...current, transaction_date: event.target.value }))} /></Field></div><Field label="Description"><Input value={transfer.description} onChange={(event) => setTransfer((current) => ({ ...current, description: event.target.value }))} placeholder="Move to cash reserve" /></Field><Field label="Notes" hint="Optional"><Textarea value={transfer.notes} onChange={(event) => setTransfer((current) => ({ ...current, notes: event.target.value }))} /></Field><Button size="lg" onClick={saveTransfer}><ArrowLeftRight size={17} />Record transfer</Button></div>}</Modal>;
}
