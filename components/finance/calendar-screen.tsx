"use client";

import { useMemo, useState } from "react";
import { addMonths, eachDayOfInterval, endOfMonth, endOfWeek, format, isSameMonth, startOfMonth, startOfWeek, subMonths } from "date-fns";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { Card, EmptyState } from "@/components/ui/primitives";
import { Button } from "@/components/ui/button";
import { useFinance } from "./finance-provider";
import { formatMoney, monetary } from "@/lib/finance/calculations";
import { TransactionList } from "./transaction-list";

export function CalendarScreen() { const { transactions, settings } = useFinance(); const [month, setMonth] = useState(new Date()); const [selected, setSelected] = useState<string | null>(null); const start = startOfWeek(startOfMonth(month), { weekStartsOn: 1 }), end = endOfWeek(endOfMonth(month), { weekStartsOn: 1 }); const days = eachDayOfInterval({ start, end }); const expenses = transactions.filter((item) => item.type === "expense"); const dayTotals = useMemo(() => new Map(days.map((day) => { const key = format(day, "yyyy-MM-dd"); return [key, expenses.filter((item) => item.transaction_date === key).reduce((sum, item) => sum + monetary(item.amount), 0)]; })), [days, expenses]); const max = Math.max(...dayTotals.values(), 1); const selectedRows = selected ? transactions.filter((item) => item.transaction_date === selected) : []; const currency = settings?.currency ?? "USD"; return <div className="grid gap-7"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-lime-300">Calendar</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-tight">See the rhythm of your spending.</h1><p className="mt-2 text-sm text-[var(--muted)]">Darker days have more expenses. Select any day to inspect its ledger.</p></div><div className="flex items-center rounded-xl bg-[var(--raised)] p-1"><button onClick={() => setMonth(subMonths(month, 1))} aria-label="Previous month" className="grid h-8 w-8 place-items-center rounded-lg hover:bg-[var(--panel)]"><ChevronLeft size={16} /></button><span className="min-w-36 px-2 text-center text-sm font-medium">{format(month, "MMMM yyyy")}</span><button onClick={() => setMonth(addMonths(month, 1))} aria-label="Next month" className="grid h-8 w-8 place-items-center rounded-lg hover:bg-[var(--panel)]"><ChevronRight size={16} /></button></div></div><Card className="overflow-hidden p-3 sm:p-5"><div className="mb-3 grid grid-cols-7 text-center text-[11px] font-medium uppercase tracking-[.12em] text-[var(--muted)]">{["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"].map((day) => <span key={day}>{day}</span>)}</div><div className="finwise-stagger grid grid-cols-7 gap-1.5 sm:gap-2">{days.map((day) => { const key = format(day, "yyyy-MM-dd"), amount = dayTotals.get(key) ?? 0, isCurrent = isSameMonth(day, month), intensity = amount / max; return <button key={key} onClick={() => setSelected(key)} className={`relative min-h-16 rounded-xl p-2 text-left transition hover:ring-1 hover:ring-lime-300/50 sm:min-h-24 ${!isCurrent ? "opacity-30" : ""} ${selected === key ? "ring-1 ring-lime-300" : ""}`} style={{ backgroundColor: amount ? `rgba(198, 161, 90, ${.08 + intensity * .34})` : "var(--raised)" }}><span className="text-xs text-[var(--muted)]">{format(day, "d")}</span>{amount > 0 && <span className="absolute bottom-2 left-2 right-2 truncate font-mono text-[10px] text-[var(--ink)] sm:text-xs">{formatMoney(amount, currency, true)}</span>}</button>; })}</div></Card>{selected ? <div><div className="mb-3 flex items-center justify-between"><div><h2 className="font-display text-lg font-semibold">{format(new Date(`${selected}T12:00:00`), "EEEE, MMMM d")}</h2><p className="text-xs text-[var(--muted)]">{formatMoney(dayTotals.get(selected) ?? 0, currency)} total expenses · {selectedRows.length} ledger entries</p></div><Button variant="ghost" size="sm" onClick={() => setSelected(null)}>Clear day</Button></div><TransactionList transactions={selectedRows} /></div> : <Card><EmptyState title="Select a day" description="Choose any day in the calendar to see its total spending and matching transactions." /></Card>}</div>; }
