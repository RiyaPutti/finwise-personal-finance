/**
 * Default preference assigned only when a user has not selected a currency yet.
 * All persisted settings and account currencies remain editable three-letter ISO codes.
 */
export const DEFAULT_WORKSPACE_CURRENCY = "INR";
